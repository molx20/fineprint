# Production Environment Variables for FinePrint Backend
# Copy this to Railway's environment variables settings

# OpenAI Configuration (REQUIRED)
OPENAI_API_KEY=sk-your-production-api-key-here
OPENAI_MODEL=gpt-4o

# Server Configuration
# Railway automatically sets PORT - don't override it
# PORT will be set by Railway (usually 8000 or dynamic)
HOST=0.0.0.0
DEBUG=false
DISABLE_SCAN_LIMITS=false

# Database Configuration
# Railway automatically provides DATABASE_URL when you add PostgreSQL
# Format: postgresql://user:password@host:port/dbname
# DATABASE_URL is auto-injected by Railway, no need to set manually

# CORS Configuration
# Replace with your actual Railway domain after deployment
# Example: https://fineprint-backend-production.up.railway.app
CORS_ORIGINS=https://your-railway-domain.up.railway.app

# Scraping Configuration
ENABLE_DYNAMIC_SCRAPING=true
DYNAMIC_SCRAPING_TIMEOUT=10
STATIC_CONTENT_THRESHOLD=200
